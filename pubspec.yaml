name: auth_firebase
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.1.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.10.0  # Verifica si tienes la versión correcta de firebase_core
  cloud_firestore: ^4.10.0  # Asegúrate de usar una versión compatible de cloud_firestore
  firebase_auth: ^4.5.0    # Si no lo tienes, también debes agregar firebase_auth
  firebase_auth_web: ^5.8.14


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true